# Dev TestFlight build marker

This file exists only to open a PR so the **TestFlight PR** (Dev flavor) workflow
can build main's current content as `WT Dev`. The Dev track builds a PR head, not
`main` — see `docs/ci/testflight.md`. Safe to delete once the build is confirmed.
