# Visual brainstorming companion mockups
.superpowers/

# Secrets (xcconfig with Google Client ID, etc.)
Secrets.xcconfig

# macOS
.DS_Store

# Claude Code — machine-local settings and nested worktrees (not for the shared repo).
# Skills are shared so cloud sessions (which clone the repo) can use them.
.claude/*
!.claude/skills/
.claude/settings.local.json
.claude/worktrees/

# Codex, XcodeBuildMCP, and design-tool local state
.codex/
.xcodebuildmcp/
.impeccable/

# Xcode user state
xcuserdata/
*.xcuserstate

# Swift Package Manager
.build/
.swiftpm/

# Python (uv virtualenv + bytecode caches)
.venv/
__pycache__/
*.pyc

# Ralph loop build/screenshot artifacts
ralph/.artifacts/
ralph/reports/
.ralph-dd/

# Local troubleshooting logs
error-logs.txt

# Node tooling for the Sandcastle agent pipeline
node_modules/
