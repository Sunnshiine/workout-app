# Visual brainstorming companion mockups
.superpowers/

# Secrets (xcconfig with Google Client ID, etc.)
Secrets.xcconfig

# macOS
.DS_Store

# Claude Code — machine-local settings, nested worktrees, and agent scratch such as decision
# trails (not for the shared repo).
.claude/settings.local.json
.claude/worktrees/
.claude/work/

# Codex, XcodeBuildMCP, and design-tool local state
.codex/
# XcodeBuildMCP local state — but the versioned session defaults stay tracked
.xcodebuildmcp/*
!.xcodebuildmcp/config.yaml
.impeccable/

# Xcode user state
xcuserdata/
*.xcuserstate

# Swift Package Manager
.build/
.swiftpm/

# workout CLI default home (WorkoutCLI/README.md)
.workout-cli/

# Python (uv virtualenv + bytecode caches)
.venv/
__pycache__/
*.pyc

# Ralph loop build/screenshot artifacts
ralph/.artifacts/
ralph/reports/
.ralph-dd/

# Local troubleshooting logs
error-logs.txt

# Node tooling for the Sandcastle agent pipeline
node_modules/

# scripts/crap.sh drops this at the repo root when it runs the coverage build
default.profraw
