# Archive

This directory contains historical planning artifacts that have already been implemented or superseded.

Do not use archived plans as implementation guidance. For current behavior and future work, use:

- `CONTEXT.md` for domain language
- `docs/adr/` for architectural decisions
- `docs/specs/` for current engineering specs
- GitHub issues for active work
