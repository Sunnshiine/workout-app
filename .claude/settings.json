{
  "enabledPlugins": {
    "superpowers@claude-plugins-official": true,
    "swift-core-skills@swift-ios-skills": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.swift) /opt/homebrew/bin/swift-format -i \"$f\"; /opt/homebrew/bin/swiftlint lint --fix \"$f\" ;; esac; } 2>/dev/null || true",
            "statusMessage": "Formatting Swift file..."
          }
        ]
      }
    ]
  }
}
