# Claude Code auth for local sandcastle runs — set ONE of the two.
# Generate an OAuth token with: claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=
ANTHROPIC_API_KEY=
# GitHub token for gh CLI inside sandboxes (issues, PRs).
GH_TOKEN=
